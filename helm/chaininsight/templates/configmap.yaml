apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "chaininsight.fullname" . }}-config
  labels:
    {{- include "chaininsight.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.backend.env }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
