# WARNING: This file contains placeholder values only.
# DO NOT commit real secrets to version control.
# Use SealedSecrets, ExternalSecrets, or a secrets manager in production.
apiVersion: v1
kind: Secret
metadata:
  name: chaininsight-secret
  namespace: chaininsight
  labels:
    app.kubernetes.io/name: chaininsight
type: Opaque
stringData:
  # IMPORTANT: Replace these placeholder values before deploying!
  API_KEY: "REPLACE_ME"
  POSTGRES_PASSWORD: "REPLACE_ME"
  REDIS_PASSWORD: "REPLACE_ME"
  DATABASE_URL: "REPLACE_ME"
  REDIS_URL: "REPLACE_ME"
